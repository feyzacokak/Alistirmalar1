x=input("3 veya 4 basamaklı bir sayı girin.")
if len(x)==3 and str(x)[0]==str(x)[2] or len(x)==4 and str(x)[0]==str(x)[3] and str(x)[1]==str(x)[2] :
        print("bu bir polindromik sayıdır.")
else:
    print("bu polindromik sayı değildir.")
